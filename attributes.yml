parent:
  label: is element of

base_ring:
  label: Base Ring
  when: has_base

category:
  label: Category

example:
  label: Example

an_instance:
  label: A [typical] instance

super_categories:
  pass
  
cardinality:
  category: FiniteEnumeratedSet

multiplication_table:
  category: FiniteSemigroups
  when: cardinality < 21

conjugate:
  category: Partition

hook_lengths:
  category: Partition

polynomial:
  category: FiniteField

evaluation:
  category: Tableau
  nonwhen: is_standard

is_standard:
  category: Tableau
  noncategory: StandardTableau
